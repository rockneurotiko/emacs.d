(use-package exwm
    :disabled t
    :ensure t
    :config
    (require 'exwm-config)
    (exwm-config-default))

;; (require 'exwm)
;; (require 'exwm-config)
;; (exwm-config-default)

(provide 'exwm-settings)
